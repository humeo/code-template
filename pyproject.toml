[project]
name = "code-template"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "questionary>=2.1.1",
    "rich>=14.3.3",
]

[tool.uv.workspace]
members = [
    "ddd",
]
